ALTER SESSION SET CONTAINER=XEPDB1;

CREATE TABLE YABBA.DOMAIN (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR2(50) NOT NULL,
    description VARCHAR2(200) NULL,
    PRIMARY KEY(id)
);

